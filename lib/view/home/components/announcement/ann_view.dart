import 'package:flutter/material.dart';

class AnnView extends StatelessWidget {
  const AnnView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}